# Copy to .env, fill the 3 lines marked below, then run:
#   docker compose -f docker-compose.prod.yml up -d

# =============================================================================
# 1. Generate a secret key (paste the output of this command):
#    python -c "import secrets; print(secrets.token_urlsafe(50))"
# =============================================================================
SECRET_KEY=

# =============================================================================
# 2. Pick a database password
# =============================================================================
POSTGRES_PASSWORD=

# =============================================================================
# 3. Connection string — only change postgres:THE_PASSWORD_ABOVE
# =============================================================================
DATABASE_URL=postgres://postgres:CHANGEME@db:5432/student_evaluation

# =============================================================================
# Everything below has sane defaults for a test run. Leave as-is.
# =============================================================================
